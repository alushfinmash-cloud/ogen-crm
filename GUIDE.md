# עוגן פיננסי CRM — מדריך עבודה מלא

## תוכן עניינים
1. [סקירה כללית](#סקירה-כללית)
2. [התקנה וחיבורים](#התקנה-וחיבורים)
3. [דשבורד](#1-דשבורד)
4. [פייפליין](#2-פייפליין)
5. [אנשי קשר](#3-אנשי-קשר)
6. [שיחות — תיבת שיחות מאוחדת](#4-שיחות--תיבת-שיחות-מאוחדת)
7. [משימות](#5-משימות)
8. [Webhook](#6-webhook)
9. [אוטומציות](#7-אוטומציות)
10. [סוכן AI](#8-סוכן-ai)
11. [ניקוד לידים](#9-ניקוד-לידים)
12. [תגובות מהירות](#10-תגובות-מהירות)
13. [הגדרות — אינטגרציות](#11-הגדרות--אינטגרציות)
14. [מבנה טכני](#מבנה-טכני)

---

## סקירה כללית

**עוגן פיננסי CRM** הוא מערכת ניהול לקוחות מקצועית לשירותים פיננסיים, בנויה בעברית מלאה עם תמיכת RTL.

**טכנולוגיות:**
- **Frontend:** Next.js 14 (App Router) + TypeScript + Tailwind CSS
- **Database:** Supabase (PostgreSQL)
- **גרפים:** Recharts
- **גרירה:** @dnd-kit (Kanban)
- **פונט:** Heebo (Google Fonts)

**מבנה הסיידבר (תפריט צד):**

| # | לשונית | תיאור קצר |
|---|--------|-----------|
| 1 | דשבורד | מבט על סטטיסטיקות וביצועים |
| 2 | פייפליין | לוח Kanban לניהול לידים |
| 3 | אנשי קשר | ניהול אנשי קשר + ייבוא/ייצוא |
| 4 | שיחות | תיבת שיחות מאוחדת (5 ערוצים) |
| 5 | משימות | ניהול משימות ותזכורות |
| 6 | Webhook | קבלת לידים ממקורות חיצוניים |
| 7 | אוטומציות | תהליכים אוטומטיים על לידים |
| 8 | סוכן AI | סוכני בינה מלאכותית |
| 9 | תגובות מהירות | תבניות הודעה מוכנות |
| 10 | ניקוד לידים | ניקוד אוטומטי לתעדוף לידים |
| 11 | הגדרות | חיבור WhatsApp, Gmail, Calendar |

---

## התקנה וחיבורים

### דרישות מקדימות
- Node.js 18+
- חשבון Supabase (חינמי)

### קובץ סביבה (.env.local)
```
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
ANTHROPIC_API_KEY=YOUR_CLAUDE_API_KEY
```

### איך מקימים Supabase
1. היכנס ל-[supabase.com](https://supabase.com) וצור פרויקט חדש
2. העתק את ה-URL ו-anon key מ **Settings → API**
3. הדבק בקובץ `.env.local` בתיקיית הפרויקט

### הרצת מיגרציות SQL
צריך להריץ את המיגרציות **בסדר הזה** ב-Supabase SQL Editor:
1. `supabase/schema.sql` — טבלאות בסיס (leads, pipeline_columns, pipelines, lead_history)
2. `supabase/migration_add_pipelines.sql` — תמיכה במספר פייפליינים
3. `supabase/migration_add_tasks.sql` — משימות ותזכורות
4. `supabase/migration_add_agents.sql` — סוכני AI
5. `supabase/migration_add_contacts.sql` — אנשי קשר
6. `supabase/migration_add_integrations.sql` — אינטגרציות
7. `supabase/migration_add_scoring.sql` — ניקוד לידים
8. `supabase/migration_add_inbox.sql` — שיחות והודעות (ערוצים, שיחות, הודעות)
9. `supabase/migration_add_inbox_v2.sql` — תגובות מהירות, הערות פנימיות, תגיות

### הפעלת המערכת
```bash
npm install
npm run dev
```
המערכת תעלה ב-`http://localhost:3000`

---

## 1. דשבורד

**נתיב:** `/dashboard` (דף הבית)

הדשבורד הוא מסך הבית של המערכת — מבט על מהיר על כל מה שקורה בעסק.

### אזורים בדשבורד:

#### כרטיסי KPI (שורה עליונה)
6 כרטיסים עם נתונים בזמן אמת:
- **לידים החודש** — סך הלידים שנכנסו בחודש הנוכחי
- **לידים חדשים השבוע** — כמות לידים מ-7 הימים האחרונים
- **אחוז המרה** — אחוז הלידים שהגיעו לסטאג' "סגירה" מתוך הכלל
- **ממוצע טיפול (ימים)** — כמה זמן בממוצע לוקח לטפל בליד
- **משימות פתוחות** — משימות בסטטוס "פתוחה" או "בטיפול"
- **משימות באיחור** — משימות שעבר תאריך היעד שלהן (מודגש באדום אם יש)

> כל כרטיס מציג **חץ למעלה/למטה** עם אחוז שינוי ביחס לתקופה הקודמת.

#### גרף לידים לפי זמן
- גרף **עמודות** או **קו** (ניתן להחליף בלחיצה)
- מציג כמות לידים שנכנסו לפי תאריך
- מתאים את עצמו לטווח התאריכים שנבחר בפילטר

#### משפך Pipeline
- בר אופקי לכל סטאג' בפייפליין
- מציג **כמות לידים** ו**אחוז מהסך הכל**
- צבע שונה לכל סטאג'
- עוזר לזהות "צוואר בקבוק" בתהליך המכירה

#### מקורות לידים
- **גרף עוגה (Donut)** — מאיפה מגיעים הלידים
- מציג חלוקה לפי מקור: אתר, פייסבוק, WhatsApp, המלצה וכו'
- לגנד עם אחוזים מימין לגרף

#### ביצועי נציגים
- **טבלה** עם עמודות: שם נציג, לידים שטופלו, המרות, משימות שהושלמו, זמן תגובה ממוצע
- **מיון** — לחיצה על כותרת עמודה ממיינת לפיה
- הנציגים המוגדרים: מנהל המערכת, נציג 1, נציג 2, נציג 3

#### פעילות אחרונה
- **פיד** של עד 20 אירועים אחרונים
- סוגי אירועים: ליד חדש, ליד עבר שלב, משימה הושלמה
- כל אירוע מציג אייקון, תיאור, שם משתמש וזמן יחסי ("לפני 3 שעות")

### פילטרים גלובליים
לחיצה על **"פילטרים"** פותחת שורת פילטרים:
- **פייפליין** — בחירת פייפליין ספציפי
- **טווח תאריכים** — היום / השבוע / החודש / רבעון / מותאם אישית
- **נציג** — סינון לפי נציג ספציפי

### רענון
- **כפתור "רענון"** — רענון ידני של כל הנתונים
- **רענון אוטומטי** כל 5 דקות

---

## 2. פייפליין

**נתיב:** `/pipeline`

הפייפליין הוא הלב של המערכת — לוח Kanban ויזואלי לניהול תהליך המכירה.

### עמודות (סטאג'ים)
ברירת מחדל: **ליד חדש** → **בטיפול** → **הצעת מחיר** → **בסגירה** → **סגור - הצלחה**

- **הוספת עמודה** — כפתור "הוסף עמודה" מימין
- **שינוי שם** — לחיצה על תפריט (⋮) בכותרת העמודה → שנה שם
- **מחיקת עמודה** — דרך אותו תפריט (רק אם ריקה מלידים)

### כרטיסי לידים
כל ליד מוצג ככרטיס עם:
- **אותיות ראשונות** של השם (עיגול צבעוני)
- **שם + טלפון**
- **ערך עסקה** (₪15K וכו')
- **מקור** (תג צבעוני — WhatsApp, אתר, פייסבוק...)
- **ניקוד** (תג צבעוני — אדום/כתום/ירוק) ← מודול ניקוד לידים
- **זמן** מאז עדכון אחרון ("לפני 2 ימים")
- **כפתורי פעולה מהירה** — התקשר, WhatsApp

> ליד שלא טופל 3+ ימים מקבל **פס כתום** בצד.

### גרירה (Drag & Drop)
- **גרירת ליד בין עמודות** — משנה את הסטאג' שלו
- **גרירה בתוך עמודה** — משנה סדר
- כל שינוי נשמר אוטומטית ונרשם בהיסטוריה

### עריכת ליד (Modal)
לחיצה על כרטיס פותחת חלון עריכה עם **4 טאבים**:

#### טאב "פרטים"
- שם, טלפון, מייל
- מקור (dropdown): אתר / פייסבוק / אינסטגרם / Google Ads / WhatsApp / המלצה / שיחה קרה / אחר
- סטטוס: חדש / פעיל / בהמתנה / סגור-הצלחה / לא רלוונטי
- ערך עסקה (₪)
- עמודה בפייפליין
- הערות חופשיות
- כפתור **שמור שינויים** / **מחק ליד**

#### טאב "ניקוד"
- **מד ויזואלי** (0-100) עם צבעים: אדום (0-30), כתום (31-60), ירוק (61-100)
- **תווית**: קר / פושר / חם
- **היסטוריית שינויי ניקוד** — כל שינוי עם +/- נקודות, סיבה ותאריך

#### טאב "משימות"
- רשימת כל המשימות המקושרות לליד
- צבעי עדיפות ותאריכי יעד
- סימון משימות באיחור

#### טאב "היסטוריה"
- ציר זמן של כל השינויים בליד
- יצירה, עדכונים, מעברי עמודות
- שם המשתמש + תאריך לכל פעולה

### פעולות מהירות (בכותרת הליד)
- **התקשר** — פותח חייגן
- **WhatsApp** — פותח שיחת WA עם המספר
- **שלח מייל** — פותח לקוח מייל

### ניהול פייפליינים
- **בורר פייפליינים** בראש הדף — ניתן ליצור מספר פייפליינים
- **הוספת פייפליין חדש** — שם + צבע
- **שינוי שם פייפליין** — לחיצה כפולה

### סיכום שורה עליונה
- כמות לידים בפייפליין
- סכום פוטנציאל כספי (₪)

---

## 3. אנשי קשר

**נתיב:** `/contacts`

מאגר מרכזי של כל אנשי הקשר במערכת.

### טבלת אנשי קשר
עמודות: שם פרטי, שם משפחה, טלפון, מייל, מקור, תגיות, תאריך יצירה

- **מיון** — לחיצה על כותרת עמודה (שם, טלפון, מייל, תאריך)
- **חיפוש** — שדה חיפוש חופשי (שם, טלפון, מייל)
- **סינון לפי תגית** — בחירת תגית מרשימה
- **עימוד** — עד 1000 אנשי קשר בעמוד

### הוספת איש קשר
כפתור **"+"** → טופס עם: שם פרטי, שם משפחה, טלפון, מייל, מקור, תגיות

> **ייחוד לפי טלפון**: אם מספר הטלפון כבר קיים, הפרטים יתעדכנו (upsert).

### עריכת איש קשר (Modal)
לחיצה על שורה פותחת חלון עם **4 טאבים**:

#### טאב "פרטים"
- עריכת כל השדות
- **ניהול תגיות** — הוספה ומחיקה של תגיות (chips)

#### טאב "לידים"
- כל הלידים המקושרים (לפי מספר טלפון)
- מציג שם, סטטוס, תאריך

#### טאב "משימות"
- משימות המקושרות לאיש קשר

#### טאב "הערות"
- הוספת הערות חופשיות עם חותמת זמן
- מחיקת הערות

### ייבוא אנשי קשר
כפתור **"ייבוא"** → **אשף 5 שלבים**:
1. **הוראות** — הסבר + דוגמה לפורמט הקובץ
2. **העלאה** — גרירת קובץ Excel (xlsx/xls) או CSV
3. **מיפוי עמודות** — התאמת עמודות הקובץ לשדות המערכת (זיהוי אוטומטי של שמות בעברית ובאנגלית)
4. **תצוגה מקדימה** — 5 שורות ראשונות לבדיקה
5. **תוצאות** — סיכום: נוצרו / עודכנו / נכשלו

### ייצוא אנשי קשר
כפתור **"ייצוא"** → הורדת קובץ CSV עם כל אנשי הקשר
> הקובץ כולל BOM לתמיכה בעברית ב-Excel.

### חיבור אוטומטי
כשליד חדש נכנס דרך Webhook עם מספר טלפון — **איש קשר נוצר אוטומטית**.

---

## 4. שיחות — תיבת שיחות מאוחדת

**נתיב:** `/inbox`

מערכת שיחות מרכזית — כל ערוצי התקשורת עם הלקוחות במקום אחד.

### ערוצים נתמכים

| ערוץ | שליחה | קבלה | Webhook |
|------|-------|------|---------|
| WhatsApp | ✅ | ✅ | `/api/inbox/webhook` |
| Facebook Messenger | ✅ | ✅ | `/api/inbox/webhook/facebook` |
| Instagram DM | ✅ | ✅ | `/api/inbox/webhook/instagram` |
| Gmail | ✅ | ✅ | `/api/inbox/webhook/gmail` |
| SMS (Twilio) | ✅ | ✅ | `/api/inbox/webhook/sms` |

### ממשק השיחות

#### פאנל שיחות (ימין)
- **רשימת שיחות** — מסודרת לפי הודעה אחרונה
- **Badge הודעות** — מספר הודעות שלא נקראו (עיגול כחול)
- **אווטאר** — אותיות ראשונות + אייקון ערוץ (💬/📘/📸/📧/📱)
- **חיפוש** — חיפוש שיחות לפי שם/טלפון
- **סינון** — לפי ערוץ (dropdown) ולפי סטטוס (פתוחה/סגורה/ארכיון)

#### כפתורי כותרת
- **🔔 הגדרות התראות** — צלילים, התראות דפדפן, עוצמה
- **🔍 חיפוש בהודעות** — חיפוש גלובלי בתוכן כל ההודעות
- **🔄 רענן** — רענון ידני
- **📎 סינון** — פילטרים
- **➕ שיחה חדשה** — יצירת שיחה חדשה

#### שיחה חדשה
לחיצה על ➕ פותחת טופס:
- שם איש קשר (חובה)
- מספר טלפון
- אימייל
- בחירת ערוץ (WhatsApp/Facebook/Instagram/Gmail/SMS...)

#### חלון צ'אט (שמאל)
- **כותרת** — שם + טלפון/מייל + ערוץ + תגיות
- **כפתורים**: הערות 📋, טלפון 📞, WhatsApp, תפריט ⋮
- **הפרדת תאריכים** — קו הפרדה בין ימים שונים
- **בועות הודעות** — כחולות (יוצאות) / לבנות (נכנסות)
- **סטטוס הודעה** — ✓ (נשלח) / ✓✓ (נמסר) / ✓✓ כחול (נקרא) / ✗ (נכשל)
- **העתקת הודעה** — כפתור בריחוף על הודעה
- **תצוגת מדיה** — תמונות מוצגות בתוך הצ'אט

### תגובות מהירות (⚡)
- לחיצה על ⚡ פותחת פאנל עם תבניות מוכנות
- **חיפוש** בתבניות
- **קיצורי מקלדת** — הקלד `/hi`, `/thanks`, `/meet` וכו' ישירות בשדה ההקלדה
- **שם + קטגוריה + קיצור + תוכן** לכל תבנית
- **שימוש** — לחיצה על תבנית ממלאת את שדה ההקלדה

### הערות פנימיות (📋)
- לחיצה על 📋 בכותרת פותחת פאנל צד
- **תגיות** — הוספת תגיות צבעוניות לשיחה (לחיצה להסרה)
- **הערות** — כתיבת הערות פנימיות שהלקוח לא רואה
- כל הערה מציגה: תוכן + שם כותב + תאריך ושעה
- מחיקת הערות בריחוף

### ייצוא שיחה
מתפריט ⋮ בכותרת השיחה:
- **ייצוא CSV** — קובץ CSV עם כל ההודעות (תומך עברית באקסל)
- **ייצוא טקסט** — קובץ TXT קריא עם הפרדת תאריכים

### חיפוש גלובלי (🔍)
- לחיצה על 🔍 פותחת פאנל חיפוש
- **חיפוש בתוכן כל ההודעות** מכל השיחות
- תוצאות מציגות: שם + תאריך + ערוץ + תוכן
- **לחיצה על תוצאה** פותחת את השיחה

### התראות
- **צלילי התראה** — "דינג" להודעה חדשה, "דינג-דונג" לשיחה חדשה
- **התראות דפדפן** — Push notifications כשהחלון לא בפוקוס
- **כותרת הדף** — מציג `(3) עוגן פיננסי CRM` כשיש הודעות שלא נקראו
- **Badge בסייד-בר** — מספר הודעות שלא נקראו ליד "שיחות"

#### הגדרות התראות (🔔)
לחיצה על 🔔 פותחת פאנל הגדרות:
- **צלילי התראה** — on/off
- **עוצמה** — סליידר + כפתור "בדיקה"
- **התראות דפדפן** — on/off (מבקש הרשאה)
- **הודעות נכנסות** — on/off
- **שיחות חדשות** — on/off

> ההגדרות נשמרות ב-localStorage ושורדות רענון.

### סטטיסטיקות שיחות בדשבורד
בתחתית הדשבורד מופיעות 3 כרטיסיות:
1. **סטטיסטיקות שיחות** — סה"כ / פתוחות / לא נקראו / הודעות היום + זמן תגובה ממוצע
2. **הודעות השבוע** — גרף עמודות: נכנסות (כחול) vs יוצאות (ירוק)
3. **שיחות לפי ערוץ** — בר התקדמות לכל ערוץ עם אחוזים

### Webhooks לחיבור ערוצים

#### WhatsApp
כתובת webhook: `https://YOUR_DOMAIN/api/inbox/webhook`
- GET — אימות Meta webhook (verify token)
- POST — קבלת הודעות נכנסות (תומך: טקסט, תמונה, וידאו, אודיו, מסמך, מיקום, סטיקר)

#### Facebook Messenger
כתובת webhook: `https://YOUR_DOMAIN/api/inbox/webhook/facebook`
- צריך: Page Access Token + Webhook Verify Token
- מביא פרופיל שולח אוטומטית

#### Instagram DM
כתובת webhook: `https://YOUR_DOMAIN/api/inbox/webhook/instagram`
- צריך: Instagram Access Token
- תומך: הודעות + תמונות + סטוריז

#### Gmail (Google Pub/Sub)
כתובת webhook: `https://YOUR_DOMAIN/api/inbox/webhook/gmail`
- עובד עם Google Pub/Sub push notifications
- תומך ב-refresh token אוטומטי

#### SMS (Twilio)
כתובת webhook: `https://YOUR_DOMAIN/api/inbox/webhook/sms`
- הגדר כ-Twilio SMS webhook URL
- תומך: טקסט + מדיה (MMS)

### אינטגרציה עם אוטומציות
- **API triggers** — `/api/inbox/triggers` — מפעיל workflows מאירועי שיחות
- **אירועים**: הודעה חדשה, שיחה חדשה, שיחה נסגרה, הודעה נכשלה
- **ניקוד לידים** — הודעות נכנסות מעדכנות ניקוד אוטומטית

---

## 5. משימות

**נתיב:** `/tasks`

ניהול משימות ותזכורות לנציגים.

### לוח משימות
כל משימה מוצגת כשורה עם:
- **כותרת** + תיאור
- **עדיפות** (תג צבעוני): נמוכה (ירוק) / בינונית (כחול) / גבוהה (כתום) / דחוף (אדום)
- **סטטוס**: פתוחה / בטיפול / הושלמה / בוטלה
- **נציג אחראי**
- **תאריך יעד**
- **ליד מקושר** (אופציונלי)

### סינון משימות
- **טאבים**: כל המשימות / באיחור / להיום / לפי סטטוס
- **פילטר עדיפות** — dropdown
- **פילטר נציג** — dropdown

### שינוי סטטוס מהיר
לחיצה על תג הסטטוס → תפריט dropdown → שינוי סטטוס ישירות מהטבלה

### יצירת משימה
כפתור **"משימה חדשה"** → טופס:
- כותרת (חובה)
- תיאור
- עדיפות
- נציג אחראי
- תאריך יעד
- קישור לליד (בחירה מרשימה)

> **תזכורת אוטומטית**: כשמגדירים תאריך יעד, המערכת יוצרת תזכורת 30 דקות לפני.

### התראת משימות באיחור
באנר צהוב בראש הדף כשיש משימות שעבר תאריך היעד שלהן.

### בדיקה אוטומטית
API endpoint `/api/tasks/auto-check` — יוצר משימות מערכת אוטומטיות ללידים שלא טופלו 48+ שעות.

---

## 6. Webhook

**נתיב:** `/webhook`

קבלת לידים אוטומטית ממקורות חיצוניים (אתרים, דפי נחיתה, פייסבוק וכו').

### איך זה עובד
1. **יוצרים Webhook** — המערכת מייצרת כתובת URL ייחודית
2. **מחברים** את הכתובת למקור (דף נחיתה, פייסבוק, וכו')
3. כשליד חדש מגיע → **נוצר אוטומטית** בפייפליין + איש קשר

### כתובת ה-Webhook
```
https://YOUR_DOMAIN/api/webhooks/YOUR_SLUG
```

### פורמט הנתונים (JSON)
```json
{
  "name": "שם הליד",
  "phone": "050-1234567",
  "email": "email@example.com",
  "source": "אתר אינטרנט",
  "notes": "הערות נוספות",
  "value": 15000
}
```

### ניהול Webhooks
- **יצירה** — שם + בחירת Pipeline + עמודה יעד
- **העתקת URL** — כפתור העתקה מהירה
- **הפעלה/השבתה** — toggle לכל webhook
- **מחיקה**

### לוג Webhooks
- צפייה בכל הבקשות שנכנסו
- זמן, מקור, סטטוס (הצלחה/כשלון)
- תוכן הבקשה המלא

---

## 7. אוטומציות

**נתיב:** `/workflows`

בניית תהליכים אוטומטיים שמופעלים כשליד נכנס או עובר שלב.

### יצירת Workflow
כפתור **"workflow חדש"** → בחירת:

#### טריגר (מה מפעיל)
- **ליד חדש מ-Webhook** — מופעל כשליד נכנס דרך webhook
- **ליד עבר עמודה** — מופעל כשליד עובר סטאג' בפייפליין

#### צעדים (מה לעשות)
שרשרת פעולות שמתבצעות ברצף:
- **שלח הודעת WhatsApp** — שליחת הודעה אוטומטית
- **שלח מייל** — שליחת מייל
- **צור משימה** — יצירת משימה לנציג
- **המתן X דקות/שעות** — עיכוב לפני הפעולה הבאה
- **עדכן סטטוס ליד** — שינוי סטטוס אוטומטי

### ניהול Workflows
- **הפעלה/השבתה** — toggle לכל workflow (play/pause)
- **עריכה** — שינוי טריגר וצעדים
- **מחיקה**

### דוגמה לתהליך
1. טריגר: ליד חדש מ-Webhook
2. צעד 1: שלח הודעת WhatsApp "שלום, קיבלנו את פנייתך!"
3. צעד 2: המתן 30 דקות
4. צעד 3: צור משימה "התקשר לליד חדש" לנציג 1

---

## 8. סוכן AI

**נתיב:** `/ai-agent`

מערכת סוכני בינה מלאכותית בהשראת HEYY — יצירת סוכנים חכמים שמטפלים בלידים.

### רשימת סוכנים
תצוגת כרטיסים (Grid) עם:
- **אווטאר** (אימוג'י)
- **שם ותפקיד** (מכירות, שירות, תמיכה טכנית, קבלת פנים, מענה כללי)
- **סטטוס** (פעיל/לא פעיל) — toggle
- **מודל AI** (Claude/GPT)
- כפתורי **עריכה** ו**מחיקה**

### עריכת סוכן (4 טאבים)

#### טאב "אישיות"
- שם הסוכן
- בחירת אווטאר (10 אימוג'ים)
- תפקיד (dropdown)
- מודל AI
- System Prompt — ההוראות הבסיסיות לסוכן
- **אישיות**: טון דיבור, תכונות, מטרה, עקרונות
- **מגבלות**: מה הסוכן לא יכול לעשות
- **ערוצים**: WhatsApp / מייל / צ'אט באתר

#### טאב "בסיס ידע"
3 סוגי ידע:
- **טקסטים** — מידע חופשי (תיאור חברה, מדיניות וכו')
- **שאלות ותשובות (FAQ)** — זוגות שאלה-תשובה
- **קישורים** — URL עם תיאור

#### טאב "פעולות"
הגדרת מה הסוכן יכול לעשות:
- יצירת ליד
- עדכון סטטוס
- קביעת פגישה
- העברה לנציג אנושי
- שליחת מסמך
- ניתן להפעיל/להשבית כל פעולה בנפרד

#### טאב "ביצועים"
- **כרטיסי סטטיסטיקות**: שיחות כוללות, שיחות פעילות, הועבר לנציג, שביעות רצון
- **היסטוריית שיחות** — רשימת כל השיחות עם תאריך וסטטוס

### חיבור נדרש
- **ANTHROPIC_API_KEY** בקובץ `.env.local` — למודלי Claude

---

## 9. ניקוד לידים

**נתיב:** `/scoring`

מערכת ניקוד אוטומטית לזיהוי לידים חמים ותעדוף.

### איך זה עובד
- כל ליד מקבל ניקוד **0-100**
- ניקוד משתנה אוטומטית לפי פעולות הליד
- הניקוד מוצג על **כרטיס הליד בפייפליין** ובתוך **חלון עריכת הליד**

### סולם הצבעים
| טווח | צבע | תווית |
|------|------|-------|
| 0-30 | אדום | קר ❄️ |
| 31-60 | כתום | פושר 🌤️ |
| 61-100 | ירוק | חם 🔥 |

### כללי ניקוד (ברירת מחדל)
| פעולה | ניקוד |
|-------|-------|
| ליד נכנס למערכת | +10 |
| ליד פתח הודעת WhatsApp | +15 |
| ליד השיב להודעה | +20 |
| ליד עבר לסטאג' פגישה | +25 |
| ליד לא הגיב 7 ימים | -10 |
| ליד ביטל פגישה | -15 |
| ליד עבר ללא רלוונטי | -50 |

### ניהול כללים
- **הוספת כלל** — שם + טריגר + ניקוד (חיובי או שלילי)
- **עריכה** — שינוי שם/טריגר/ניקוד
- **הפעלה/השבתה** — toggle לכל כלל
- **מחיקה**

### טריגרים זמינים
- ליד נכנס למערכת
- ליד פתח הודעת WhatsApp
- ליד השיב להודעה / למייל
- ליד פתח מייל / השיב למייל
- ליד עבר לסטאג' (פגישה / הצעת מחיר / סגירה / לא רלוונטי)
- ליד לא הגיב 7/14 ימים
- ליד ביטל פגישה
- ליד מילא טופס / לחץ על קישור
- שינוי ידני

### סף התראות
- **ליד חם** (ברירת מחדל: 70) — התראה "כדאי ליצור קשר עכשיו!"
- **ליד קר** (ברירת מחדל: 20) — התראה "ליד מתקרר — שקול פעולה"

### שימוש ב-API
ניתן להפעיל ניקוד ידנית:
```
POST /api/scoring
{
  "lead_id": "UUID",
  "trigger_type": "whatsapp_replied"
}
```
או שינוי ידני:
```
POST /api/scoring
{
  "lead_id": "UUID",
  "points_override": 15,
  "reason_override": "שיחה טובה"
}
```

---

## 10. תגובות מהירות

**נתיב:** `/quick-replies`

ניהול תבניות הודעה מוכנות לשימוש מהיר בשיחות.

### רשימת תבניות
כל תבנית מציגה:
- **כותרת** — שם התבנית
- **קטגוריה** (תג צבעוני): כללי / מכירות / תיאום / מעקב / תמיכה
- **קיצור** — קוד מקלדת (לדוגמה: `/hi`, `/thanks`)
- **תוכן** — ההודעה עצמה
- **שימוש** — כמה פעמים נעשה שימוש
- **toggle** — הפעלה/השבתה

### תבניות ברירת מחדל (8)

| כותרת | קיצור | תוכן |
|-------|-------|------|
| ברכת פתיחה | `/hi` | שלום! תודה שפנית אלינו. איך אוכל לעזור? |
| בקשת פרטים | `/details` | אשמח לקבל ממך את הפרטים הבאים: שם מלא, מספר טלפון ומייל. |
| תודה | `/thanks` | תודה רבה! נחזור אליך בהקדם. |
| בדיקה | `/check` | אני בודק את העניין ואחזור אליך עם תשובה בהקדם. |
| פגישה | `/meet` | אשמח לקבוע איתך פגישה. מתי נוח לך? |
| הצעת מחיר | `/quote` | הצעת המחיר נשלחה אליך למייל. אשמח לשמוע חוות דעתך. |
| סגירה | `/close` | תודה על הפנייה! אם תצטרך משהו נוסף, אל תהסס לפנות. |
| המתנה | `/wait` | ממתין לתשובתך. אל תהסס לפנות בכל שאלה. |

### פעולות
- **תבנית חדשה** — כותרת + תוכן + קטגוריה + קיצור
- **עריכה** — שינוי כל השדות
- **מחיקה** — עם אישור
- **הפעלה/השבתה** — toggle
- **סינון** — לפי קטגוריה + חיפוש חופשי

### שימוש בשיחות
1. **כפתור ⚡** — פותח את רשימת התגובות המהירות מתוך הצ'אט
2. **הקלדת קיצור** — הקלד `/hi` בשדה ההקלדה → מוחלף בתוכן התבנית
3. **לחיצה על תבנית** → ממלאת את שדה ההקלדה (לא שולחת אוטומטית)

---

## 11. הגדרות — אינטגרציות

**נתיב:** `/settings`

חיבור המערכת לשירותים חיצוניים.

### WhatsApp Business

#### מה צריך
- חשבון **Meta for Developers**
- אפליקציית WhatsApp Business
- **Phone Number ID** — מזהה מספר הטלפון
- **Access Token** — טוקן גישה
- **Webhook Verify Token** — טוקן אימות (אתה בוחר)

#### איך מחברים
1. היכנס ל-[developers.facebook.com](https://developers.facebook.com)
2. צור אפליקציה חדשה → הוסף מוצר WhatsApp
3. ב-WhatsApp → Getting Started → העתק **Phone Number ID** ו-**Access Token**
4. הדבק בהגדרות המערכת → כרטיס WhatsApp Business
5. לחץ **"בדוק חיבור"** לאימות
6. לחץ **"שמור"**

#### תבניות WhatsApp
- **ניהול תבניות** — יצירה, עריכה, מחיקה
- **משתנים**: `{name}`, `{phone}`, `{email}`, `{source}`, `{date}`
- המשתנים מוחלפים אוטומטית בנתוני הליד בעת השליחה

### Gmail

#### מה צריך
- חשבון **Google Cloud Console**
- OAuth 2.0 Client ID + Client Secret
- הפעלת Gmail API

#### איך מחברים
1. היכנס ל-[console.cloud.google.com](https://console.cloud.google.com)
2. צור פרויקט חדש → הפעל **Gmail API**
3. ב-Credentials → Create OAuth 2.0 Client ID
4. העתק **Client ID** ו-**Client Secret**
5. הזן את כתובת המייל שממנה תישלחנה הודעות
6. הדבק בהגדרות → כרטיס Gmail → שמור

#### משתנים זמינים במיילים
`{name}`, `{phone}`, `{email}`, `{source}`, `{date}`

### Google Calendar

#### מה צריך
- אותו פרויקט Google Cloud
- הפעלת **Google Calendar API**
- **Calendar ID** (בדרך כלל כתובת המייל שלך)

#### איך מחברים
1. ב-Google Cloud Console → הפעל **Google Calendar API**
2. השתמש באותם OAuth credentials
3. הכנס את ה-Calendar ID (מ-Google Calendar → Settings → Calendar ID)
4. הדבק בהגדרות → כרטיס Google Calendar → שמור

---

## מבנה טכני

### מבנה תיקיות
```
ogen-crm/
├── app/                          # דפים (Next.js App Router)
│   ├── page.tsx                  # דף בית → redirect ל-/dashboard
│   ├── layout.tsx                # Layout ראשי (Heebo font, RTL)
│   ├── dashboard/page.tsx        # דשבורד
│   ├── pipeline/page.tsx         # פייפליין
│   ├── contacts/page.tsx         # אנשי קשר
│   ├── inbox/page.tsx            # תיבת שיחות
│   ├── tasks/page.tsx            # משימות
│   ├── webhook/page.tsx          # Webhook
│   ├── workflows/page.tsx        # אוטומציות
│   ├── ai-agent/page.tsx         # סוכן AI — רשימה
│   ├── ai-agent/[id]/page.tsx    # סוכן AI — עריכה
│   ├── quick-replies/page.tsx    # ניהול תגובות מהירות
│   ├── scoring/page.tsx          # ניקוד לידים
│   ├── settings/page.tsx         # הגדרות
│   └── api/                      # API Routes
│       ├── dashboard/            # GET — סטטיסטיקות
│       ├── tasks/                # CRUD משימות
│       ├── contacts/             # CRUD אנשי קשר + import/export
│       ├── agents/               # CRUD סוכני AI
│       ├── scoring/              # ניקוד + כללים
│       ├── integrations/         # WhatsApp/Gmail/Calendar
│       ├── webhooks/             # Webhook endpoints
│       ├── workflows/            # Workflow CRUD + trigger
│       └── inbox/                # תיבת שיחות
│           ├── conversations/    # CRUD שיחות
│           ├── messages/         # GET/POST הודעות + שליחה
│           ├── quick-replies/    # CRUD תגובות מהירות
│           ├── notes/            # CRUD הערות פנימיות
│           ├── tags/             # CRUD תגיות שיחה
│           ├── search/           # חיפוש גלובלי בהודעות
│           ├── export/           # ייצוא שיחה (CSV/TXT)
│           ├── stats/            # סטטיסטיקות שיחות
│           ├── triggers/         # טריגרים לאוטומציות
│           └── webhook/          # Webhooks
│               ├── route.ts      # WhatsApp
│               ├── facebook/     # Facebook Messenger
│               ├── instagram/    # Instagram DM
│               ├── gmail/        # Gmail (Pub/Sub)
│               └── sms/          # SMS (Twilio)
├── components/                   # קומפוננטות React
│   ├── Sidebar.tsx               # תפריט צד (עם badge הודעות)
│   ├── Dashboard.tsx             # דשבורד + סטטיסטיקות שיחות
│   ├── KanbanBoard.tsx           # לוח Kanban ראשי
│   ├── KanbanColumn.tsx          # עמודת Kanban
│   ├── LeadCard.tsx              # כרטיס ליד
│   ├── LeadModal.tsx             # חלון עריכת ליד (4 טאבים)
│   ├── ContactList.tsx           # טבלת אנשי קשר
│   ├── ContactModal.tsx          # חלון עריכת איש קשר
│   ├── ImportContacts.tsx        # אשף ייבוא 5 שלבים
│   ├── Inbox.tsx                 # תיבת שיחות מאוחדת (צ'אט מלא)
│   ├── QuickRepliesManager.tsx   # ניהול תגובות מהירות
│   ├── TaskDashboard.tsx         # לוח משימות
│   ├── CreateTaskModal.tsx       # יצירת/עריכת משימה
│   ├── AgentList.tsx             # רשימת סוכני AI
│   ├── AgentEditor.tsx           # עריכת סוכן (4 טאבים)
│   ├── ScoringRulesManager.tsx   # ניהול כללי ניקוד
│   └── IntegrationsPage.tsx      # הגדרות אינטגרציות
├── lib/                          # טיפוסים ו-utilities
│   ├── types.ts                  # Lead, Pipeline, Column
│   ├── supabase.ts               # Supabase client
│   ├── task-types.ts             # Task interfaces
│   ├── agent-types.ts            # Agent interfaces
│   ├── contact-types.ts          # Contact interfaces
│   ├── integration-types.ts      # Integration interfaces
│   ├── scoring-types.ts          # Scoring interfaces
│   ├── dashboard-types.ts        # Dashboard interfaces
│   ├── inbox-types.ts            # Inbox/Chat interfaces + helpers
│   └── notifications.ts          # מערכת התראות (צלילים, Push, הגדרות)
└── supabase/                     # מיגרציות SQL
    ├── schema.sql                # טבלאות בסיס
    ├── migration_add_pipelines.sql
    ├── migration_add_tasks.sql
    ├── migration_add_agents.sql
    ├── migration_add_contacts.sql
    ├── migration_add_integrations.sql
    ├── migration_add_scoring.sql
    ├── migration_add_inbox.sql       # שיחות והודעות
    └── migration_add_inbox_v2.sql    # תגובות מהירות + הערות + תגיות
```

### טבלאות Supabase

| טבלה | תיאור |
|------|-------|
| `pipelines` | פייפליינים |
| `pipeline_columns` | עמודות/סטאג'ים |
| `leads` | לידים |
| `lead_history` | היסטוריית שינויים בלידים |
| `contacts` | אנשי קשר |
| `contact_notes` | הערות לאנשי קשר |
| `tasks` | משימות |
| `task_reminders` | תזכורות |
| `agents` | סוכני AI |
| `agent_knowledge_text` | ידע טקסטואלי |
| `agent_knowledge_faq` | שאלות ותשובות |
| `agent_knowledge_links` | קישורים |
| `agent_actions` | פעולות סוכן |
| `agent_conversations` | שיחות |
| `agent_messages` | הודעות בשיחות |
| `integrations` | הגדרות אינטגרציות |
| `whatsapp_templates` | תבניות WhatsApp |
| `whatsapp_messages` | הודעות WhatsApp |
| `emails_sent` | מיילים שנשלחו |
| `calendar_events` | אירועי יומן |
| `scoring_rules` | כללי ניקוד |
| `lead_scores` | ניקוד נוכחי לכל ליד |
| `lead_score_history` | היסטוריית ניקוד |
| `channels` | ערוצי תקשורת |
| `conversations` | שיחות |
| `messages` | הודעות בשיחות |
| `quick_replies` | תגובות מהירות |
| `conversation_notes` | הערות פנימיות |
| `conversation_tags` | תגיות לשיחות |

### נציגים מוגדרים במערכת
- מנהל המערכת
- נציג 1
- נציג 2
- נציג 3

> ניתן לשנות ב-`lib/task-types.ts` → `ASSIGNEES`

---

## טיפים לעבודה יומית

1. **התחל את היום בדשבורד** — בדוק KPIs, משימות באיחור, סטטיסטיקות שיחות
2. **בדוק שיחות** — עבור ל"שיחות" וטפל בהודעות שלא נקראו
3. **השתמש בתגובות מהירות** — הקלד `/hi` או `/thanks` לתגובות מהירות
4. **עבוד על הפייפליין** — גרור לידים בין סטאג'ים ככל שהם מתקדמים
5. **בדוק ניקוד** — התמקד בלידים עם ניקוד גבוה (ירוק/חם)
6. **סמן משימות** — שנה סטטוס ל"הושלמה" כשסיימת
7. **הוסף הערות** — כתוב הערות פנימיות על שיחות חשובות
8. **תייג שיחות** — השתמש בתגיות לסיווג שיחות (VIP, דחוף, המתנה)
9. **ייבא אנשי קשר** — שמור על מאגר מעודכן דרך ייבוא Excel
10. **הגדר Webhooks** — חבר דפי נחיתה ורשתות חברתיות
11. **בנה אוטומציות** — הפוך תהליכים חוזרים לאוטומטיים
12. **ייצא שיחות** — שמור גיבוי של שיחות חשובות (CSV/TXT)

---

*נבנה עם Next.js 14 + Supabase + Tailwind CSS*
*עוגן פיננסי CRM &copy; 2026*
